#### Einrichtung

Umgebungsvariable "DWBK_DB_DIR": muss auf Ordner zeigen, in welchem die DB (dwbk_db.gpkg) erzeugt wird.

## Start GUI test
mvn clean javafx:run

# debug mode!
mvn clean javafx:run@debug
mvn clean javafx:run@run